'use strict'
const arr = ['a', 'b', 'c']
// for (let [index, item] of arr.entries()) {
//   console.log('index:' + index + 'item:' + item)
// }
for (let item of arr) {
  console.log('item:' + item)
}
